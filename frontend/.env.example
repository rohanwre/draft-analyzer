# This app runs entirely client-side against the static data snapshot bundled in
# public/data/ (see scripts/export_static_data.py) — no backend, no env vars needed.
# Re-run the export script after any pipeline recompute that changes the underlying
# DB tables, then rebuild/redeploy to pick up the new data.
